import styles from "./szavazas.module.css";

export const metadata = {
	title: "Parlament Figyelő",
	description: "Készítette",
};

export default function Layout({children}) {
	return <>{children}</>;
}
